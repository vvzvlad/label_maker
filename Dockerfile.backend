FROM python:3.11-slim

WORKDIR /app

COPY requirements-backend.txt .
RUN pip install --no-cache-dir -r requirements-backend.txt
RUN playwright install --with-deps chromium

COPY main.py .
COPY src/ ./src/
COPY static/ ./static/

ENV ENABLE_PDF_API=1

CMD ["python", "main.py"]
